version: '3.8'

services:
  app:
    build: .
    container_name: journal-ippolitovka
    restart: always
    ports:
      - "80:3000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - NODE_ENV=production